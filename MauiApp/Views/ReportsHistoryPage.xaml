<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp.Views.ReportsHistoryPage"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False"
             Background="Transparent">

    <Grid>

        <!-- 🔸 Background Gradient -->
        <BoxView>
            <BoxView.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="{StaticResource TopCoral}" Offset="0.0" />
                    <GradientStop Color="{StaticResource BottomCoral}" Offset="1.0" />
                </LinearGradientBrush>
            </BoxView.Background>
        </BoxView>

        <!-- 🔹 Header -->
        <Grid HeightRequest="120"
              VerticalOptions="Start"
              HorizontalOptions="FillAndExpand">

            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="56"/>
                <RowDefinition Height="24"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="1" Padding="10,0,15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- ⬅️ Back Button -->
                <Image Grid.Column="0"
                       Source="ic_arrow_back.svg"
                       WidthRequest="22"
                       HeightRequest="22"
                       VerticalOptions="Center"
                       HorizontalOptions="Start">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnBackButtonClicked" />
                    </Image.GestureRecognizers>
                </Image>

                <Label Grid.Column="1"
                       Text="Reports History"
                       FontSize="18"
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />
            </Grid>
        </Grid>

        <!-- 🧾 Reports List -->
        <ScrollView Margin="0,110,0,0"
                    Padding="15,10,15,80">
            <CollectionView x:Name="ReportsCollectionView"
                            ItemsSource="{Binding Reports}"
                            BackgroundColor="Transparent"
                            SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical"
                                       ItemSpacing="10" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="White"
                               CornerRadius="12"
                               Padding="12"
                               HasShadow="True">
                            <Grid ColumnDefinitions="Auto,*,Auto"
                                  ColumnSpacing="10">

                                <!-- 📄 PDF Icon -->
                                <Image Grid.Column="0"
                                       Source="ic_pdf_icon.png"
                                       WidthRequest="28"
                                       HeightRequest="28"
                                       VerticalOptions="Center" />

                                <!-- 📘 Report Info -->
                                <StackLayout Grid.Column="1"
                                             Spacing="3"
                                             VerticalOptions="Center">
                                    <Label Text="{Binding FileName}"
                                           FontSize="15"
                                           FontAttributes="Bold"
                                           TextColor="Black"
                                           LineBreakMode="TailTruncation" />
                                    <Label Text="{Binding CreatedDate, StringFormat='Generated: {0:MMM dd, yyyy • HH:mm}'}"
                                           FontSize="13"
                                           TextColor="#666" />
                                    <Label Text="{Binding ImageCount, StringFormat='{0} images'}"
                                           FontSize="12"
                                           TextColor="#999" />
                                </StackLayout>

                                <!-- 🎯 Action Icons -->
                                <StackLayout Grid.Column="2"
                                             Orientation="Horizontal"
                                             Spacing="12"
                                             VerticalOptions="Center">

                                    <!-- 👁 View Icon -->
                                    <ImageButton Source="ic_eye_icon.png"
                                                 BackgroundColor="Transparent"
                                                 WidthRequest="24"
                                                 HeightRequest="24"
                                                 Clicked="OnViewReportClicked"
                                                 CommandParameter="{Binding FilePath}" />

                                    <!-- 🔗 Share Icon -->
                                    <ImageButton Source="ic_share_icon.png"
                                                 BackgroundColor="Transparent"
                                                 WidthRequest="24"
                                                 HeightRequest="24"
                                                 Clicked="OnShareReportClicked"
                                                 CommandParameter="{Binding FilePath}" />
                                </StackLayout>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>

        <!-- 📄 Empty State -->
        <Frame x:Name="EmptyStateFrame"
               BackgroundColor="White"
               CornerRadius="12"
               Padding="30"
               HasShadow="True"
               IsVisible="False"
               VerticalOptions="Center"
               HorizontalOptions="Center"
               Margin="30">
            <StackLayout Spacing="15"
                         HorizontalOptions="Center">
                <Image Source="ic_pdf_icon.png"
                       WidthRequest="60"
                       HeightRequest="60"
                       HorizontalOptions="Center" />
                <Label Text="No Reports Generated"
                       FontSize="18"
                       FontAttributes="Bold"
                       TextColor="Black"
                       HorizontalOptions="Center" />
                <Label Text="Generate your first PDF report to see it here"
                       FontSize="14"
                       TextColor="#666"
                       HorizontalOptions="Center" />
            </StackLayout>
        </Frame>

        <!-- 🔘 Bottom Button -->
        <Frame VerticalOptions="End"
               BackgroundColor="Transparent"
               Padding="20,15"
               HasShadow="False">
            <Button Text="Generate New Report"
                    FontSize="16"
                    FontAttributes="Bold"
                    BackgroundColor="{StaticResource TopCoral}"
                    TextColor="White"
                    HeightRequest="50"
                    CornerRadius="25"
                    Clicked="OnGenerateNewReportClicked" />
        </Frame>

    </Grid>
</ContentPage>
